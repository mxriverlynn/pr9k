# Implementation Iteration History: Phase 3 — Interactive Error Recovery

History of how the Phase 3 implementation plan evolved across discussion rounds. Committed decisions live in [implementation-decision-log.md](implementation-decision-log.md); the primary plan lives in [../feature-implementation-plan.md](../feature-implementation-plan.md).

Feature size: **Small** (single subsystem, single language, no cross-service/auth/PII/migration — the integration gap adds code volume, not subsystems). Team cap 3 (project-manager + junior-developer + behavioral-analyst). Round cap 1. No `feature-technical-notes.md` exists for Phase 3, so the `T#-contradiction` spec-maturity class does not apply; the spec-maturity gate reduces to the spec-level threshold alone.

## R1: Parallel specialist review

- **Specialists engaged:** behavioral-analyst, junior-developer. (project-manager reserved for Step 8 synthesis; gate did not trip, so no facilitation call.)
- **New input provided:** Phase 3 feature specification, spec decision log (D1–D4), spec team findings (F1–F16), Phase 3 `.discovery-notes.md` (including the CRITICAL FINDING that Phase 2's end-to-end integration is unwired), shared Phase 2 discovery notes.
- **Claim ledger:**

| # | Claim | Supporting specialists | State | Spec-maturity |
|---|-------|------------------------|-------|---------------|
| C1 | Phase 2 happy-path is NOT runnable in cmux: `runCmuxOrchestratorWith` stubs `workflow.Run` (`cmux_pane.go:113-114`); `newFooterStateMachine`/`newKeyHandlerAdapter` have zero non-test callers; `runCmuxDisplayPane` ignores `StateHeader`/`StateLog` (`cmux_pane.go:177-193`); footer pane has no keystroke loop. | behavioral-analyst (B1–B4), junior-developer (restatement, Assumptions 1/2/5) | **Evidenced** | plan-level (sequencing) + spec-wording correction |
| C2 | `cmuxFooterRenderer` constructs a private, disconnected `KeyHandler` with an unread `actions` channel (`cmux_footer_renderer.go:22-25`); `HandleKey` handles only `?`/`esc`, no state-machine delegation. | behavioral-analyst (B3) | **Evidenced** | plan-level |
| C3 | `orchChannel` interface (`cmux_pane.go:27-32`) lacks `SendStateHeader`/`SendStateLog`/`SendStateFooter`; must be expanded for the orchestrator to push state. `FakeInteractionChannel` already implements them (Phase 2 U10). | behavioral-analyst (B5, B11-h3) | **Evidenced** | plan-level |
| C4 | Recommended sequencing: **absorb** the Phase 2 integration as foundational Phase 3 work units. Error recovery is not separable from the shared `Orchestrate` loop — the same code path emits step-success, `SetMode(ModeDone)`, AND `SetStepState(StepFailed)`/`SetMode(ModeError)`. A separate-prerequisite path creates a dependency chain. | behavioral-analyst (B-Q4 answer) | **Evidenced** (technical) | plan-level; scope/commitment aspect → user |
| C5 | The absorb-vs-prerequisite call is a scope/commitment/naming decision (what "Phase 3 done" means, demo expectations, whether Phase 2-incomplete is silently absorbed), not purely technical. Belongs to user/project-manager. | junior-developer (Q1, Q2) | **Evidenced** | plan-level → **user escalation** |
| C6 | Spec/decision-log wording ("Phase 2 already established and tested" the wired path; Preconditions "a workflow is running inside a Phase 2 cmux workspace") is true only for isolated unit-tested components, misleading for the integrated path. Needs correction regardless of option chosen. | junior-developer (Assumptions 1–5), behavioral-analyst (B1) | **Evidenced** | spec-level (resolvable via spec-wording correction the plan surfaces) |
| C7 | Race-window guarantee (spec D1) holds by design once wired: buffered `h.Actions` (cap 10) + synchronous `onModeChange` hook before `<-h.Actions` block; `concurrency.md` "prime the channel before blocking receive" is the canonical pattern. No new risk from wiring. | behavioral-analyst (B6) | **Evidenced** (positive) | plan-level |
| C8 | Retry-separator-before-output ordering (spec D3) sound by sequential execution (`orchestrate.go:135`) ONLY IF the `WriteToLog`→`SendStateLog` adapter is synchronous on the orchestrator goroutine (not dispatched async). | behavioral-analyst (B7) | **Evidenced** | plan-level (implementation constraint) |
| C9 | The orchestrator's `KeyHandler` must be constructed with the runner's `Terminate` as the cancel fn (cf. `main.go:226`); the footer renderer's `nil`-cancel `KeyHandler` (`cmux_footer_renderer.go:22`) would make quit-during-active-step fail to kill the subprocess. | behavioral-analyst (B9) | **Evidenced** | plan-level (implementation constraint) |
| C10 | Wiring ordering hazard: `SetOnModeChange` + key-adapter goroutine must be established before `workflow.Run` starts (`concurrency.md:71-78`), else a first-step `SetMode(ModeError)` could be missed. Footer keystroke goroutine must exit on ctx cancel + WaitGroup-drain (`concurrency.md:24-37`). | behavioral-analyst (B11-h1,h2) | **Evidenced** | plan-level (implementation constraint) |
| C11 | `Orchestrate` calls `h.SetMode(ModeNormal)` after `<-h.Actions` regardless of action; on the quit path this overwrites `ModeQuitting` and briefly pushes `StateFooter(ModeNormal)` before `WorkspaceDone` — a cosmetic bounded desync the spec does not explicitly address. | behavioral-analyst (B10) | **Evidenced** | spec-level (resolvable by evidence: spec D4/parent T4 already accept analogous bounded desync) |
| C12 | "No new message kinds / no new cmux calls" (spec Coordinations) is verified for components but the adapter-construction wiring is new code unit tests never exercised; flag as integration risk + add an integration test (the Phase 2 race-window test covers components in isolation, not the wired path). | junior-developer (Q3, Q4) | **Evidenced** | spec-level (assertion predates wiring) — resolvable in plan via an integration test |

- **Open Questions raised:**
  - **OQ-1 (blocking, user):** Should the Phase 3 plan **absorb** the Phase 2 end-to-end integration as foundational work units, or treat it as a **prerequisite** completed as separate Phase 2-completion work first? (C4, C5) — escalated to user with recommendation.
  - **OQ-2 (resolved by evidence):** Does the spec's "thin reuse / established and tested" wording need correction? (C6) → Yes, regardless of OQ-1's answer — the plan will record a spec-wording-correction action item / Open Item; the plan author surfaces it. Not behavior-changing (spec D1–D4 behavior stands), so no spec re-entry required.
  - **OQ-3 (resolved by evidence):** Are the spec's load-bearing guarantees (race window, retry-separator ordering, cancel-restore, bounded desync) honored by the existing components once wired? (C7, C8, C11) → Yes; C8/C9/C10 become implementation constraints captured as decisions; C11 is absorbed by the spec's existing bounded-desync acceptance (D4/parent T4).
  - **OQ-4 (resolved — no handoff needed):** junior-developer named behavioral-analyst for Q3/Q4 and concurrency-analyst conditionally. behavioral-analyst already answered Q3/Q4 (B6/B7) and covered the concurrency hazards (B11) with `concurrency.md` citations. No additional specialist required; adding one would exceed the Small team cap.
- **Spec-maturity tags:** plan-level: C1–C5, C7–C10 (9). spec-level: C6, C11, C12 (3). T#-contradiction: 0 (no T# notes exist). **Gate computation:** spec-level threshold is ≥5 spec-level findings by ≥3 distinct specialists; only 2 specialists on a Small team and only 3 spec-level findings → **gate did NOT trip.** No PM gate-trip facilitation call made.
- **Resolution source:** OQ-1 → user input (escalated). OQ-2, OQ-3, OQ-4 → evidence.
- **Decisions produced:** _(backfilled in synthesis)_
- **Changed in plan:** _(backfilled in synthesis)_
- **Project-manager next-step recommendation (deterministic):** Blocked pending user input on OQ-1 (absorb vs. prerequisite). Round cap (1) reached; no further specialist rounds. After the user answers OQ-1, proceed directly to Step 8 synthesis — all other Open Questions are evidence-resolved.
