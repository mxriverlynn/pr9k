# Implementation Iteration History: Phase 1 — Cmux Mode Launch and Workspace Lifecycle

<!--
Records how the implementation plan for Phase 1 evolved across discussion rounds. Committed decisions live in [implementation-decision-log.md](implementation-decision-log.md); the primary plan lives in [../feature-implementation-plan.md](../feature-implementation-plan.md).

`Decisions produced:` and `Changed in plan:` for each round are backfilled by the project-manager during synthesis (Step 8).
-->

## R1: Parallel specialist review (devops, behavioral, concurrency, test-engineer, junior-developer)

- **Specialists engaged:** `devops-engineer`, `behavioral-analyst`, `concurrency-analyst`, `test-engineer`, `junior-developer`
- **New input provided:** Initial feature specification, Phase 1 decision log, Phase 1 team findings, parent decision log, parent feature-technical-notes (T1–T5), parent feature spec, build-phase outline, investigation doc, and the consolidated `.discovery-notes.md` (project context: tech stack, ADRs, coding standards, code touch points, recent activity, explicitly enumerated gaps).
- **Claim ledger** (consolidated from verbatim outputs; each row names the supporting specialists and the state):

  | # | Category | Claim | Specialists | State |
  |---|----------|-------|-------------|-------|
  | 1 | Integration boundary | D9's placeholder-exit observation arm depends on cmux's API exposing per-pane process-exit notifications or a queryable pane-status endpoint — capability not confirmed by parent T1–T5 nor the cmux API table in `investigation.md`. If absent, D9 degrades to workspace-list-only, which D9's own rejected-alternatives section identifies as insufficient. | behavioral B10, junior Q5 | Evidenced |
  | 2 | Race condition | Self-close double-fire — pr9k's own `workspace.close` during shutdown triggers the workspace-list-disappearance observation, racing the in-progress shutdown sequence (potential duplicate `workspace.close` and duplicate focus-restore). Includes TOCTOU window when polling cadence puts a `workspace.list` call in flight during teardown. | concurrency C1+C6, behavioral B1 | Evidenced |
  | 3 | Race condition / shared resource | No single-flight rule on the cmux JSON-RPC socket. Polling goroutine + shutdown goroutine concurrently access one `net.Conn`. Recommendation: serialize through a single goroutine for Phase 1; defer multiplex to Phase 4 (sidebar). | concurrency C2 | Evidenced |
  | 4 | Goroutine lifecycle | Dismissal-observer goroutine has no committed cancellation discipline and no committed channel-priming guarantee. Needs `context.Context` cancellation + `WaitGroup` join + buffered dismissal channel. | concurrency C3+C7 | Evidenced |
  | 5 | Async error handling | Per-call timeout protocol-level behavior unspecified — close socket vs. abandon ID. Recommended: close connection on timeout; re-open for teardown. Avoids late-response routing hazard. | concurrency C4 | Evidenced |
  | 6 | Signal handling | D6's "second signal exits immediately" cannot be delivered if the signal-handler goroutine is blocked in a hangable cmux call (up to 10s per call per parent D27). Requires two-goroutine watchdog+cleanup pattern with the watchdog calling `os.Exit` on second signal regardless of cleanup state. | concurrency C5, devops DOR-002, behavioral B11 | Evidenced |
  | 7 | Signal handling | SIGHUP not registered in the existing `signal.Notify` call (`src/cmd/pr9k/main.go:284-285`). D6 commits all three signal kinds (SIGINT/SIGTERM/SIGHUP) to graceful shutdown on first delivery. Without registration, Go's default SIGHUP disposition kills the process immediately, contradicting D6. | devops DOR-001, behavioral B6 | Evidenced |
  | 8 | Runtime correctness | `startup()` unconditionally creates the per-run logger and `.pr9k/logs/<run-stamp>` artifact directory (`src/cmd/pr9k/main.go:96-108`). Phase 1 spec commits to "no `.pr9k/logs/` artifacts produced." Implementation must branch `main()`/`startup()` on `cfg.Cmux` to skip logger creation. | devops DOR-003, behavioral B5, junior Q8 | Evidenced |
  | 9 | Runtime correctness | Bubble Tea `program` and status-line runner are started unconditionally (`src/cmd/pr9k/main.go:191-214`). Phase 1's cmux path must NOT enter `program.Run()` (alt-screen would corrupt the launching terminal) and must NOT start the status-line runner. Implementation must branch on `cfg.Cmux` before any TUI wiring. | devops DOR-004 | Evidenced |
  | 10 | CLI | `--cmux` flag does not exist in `cli.Config` or `args.go`. Trivially adds `Cmux bool` field + `cmd.Flags().BoolVar(...)`. | devops DOR-005 | Evidenced |
  | 11 | Observability | D9 dismissal-observation polling cadence is unspecified. Risk: too-long cadence makes workspace-close feel like a hang; too-short floods the cmux socket. Recommendation: 500ms single-flight (one in-flight poll at a time), worst-case detection latency ≈ poll-cadence + per-call timeout. | devops DOR-006 | Evidenced |
  | 12 | Async behavior | Parent D15 says "every cmux API call timeout is fatal" — but D15 was specified for the launch sequence. If applied uniformly to D9 poll calls during the waiting phase, a single transient cmux slowness would tear down a healthy workspace mid-inspection. Spec must clarify scope of D15. | behavioral B3 | Evidenced |
  | 13 | CI / testability | OI-2 — Phase 1 needs a `CmuxClient` interface + `FakeClient` test double in `internal/cmuxctl` from day one. Test plan (T2) confirms mocking covers every Phase 1 behavior except the live JSON-RPC wire format, which appropriately defers to Phase 6. Build-outline OQ-2 recommendation ("mock through Phase 5") is confirmed. | devops DOR-007, test-engineer (T2 answer) | Evidenced |
  | 14 | State management | Spec's "exit zero on workspace-close gesture; non-zero on placeholder-exit" rule (User Interactions, Dismissal gestures) requires deterministic observation ordering, but D9 only commits to "either fires first." For an operator's workspace-close gesture, both events typically fire near-simultaneously (cmux removes workspace AND terminates pane processes). Whichever the implementation observes first determines the exit code → non-deterministic exit codes for the same operator action. Two routes: (a) drop the exit-code distinction; (b) add an ordering rule. | behavioral B4, devops DOR-008 | Evidenced |
  | 15 | Error propagation | "No orphan workspace from this failure mode" (Edge Cases partial-setup row) is not actually guaranteed when `workspace.close` itself fails for a non-race reason (timeout, network error). The spec's swallow-and-continue rule defeats the orphan guarantee silently. Either the guarantee needs an audit (carve out exceptions) or the operator needs a distinct error message when teardown itself fails. | behavioral B2 | Evidenced |
  | 16 | State management | Pane-spawn order across the four panes is unspecified. With orchestrator-first vs. orchestrator-last, the operator sees different partial-setup states during teardown. Minor brittleness; spec should commit to an explicit order. | behavioral B9 | Anecdotal |
  | 17 | Dependency hygiene | D11's accepted set `[a-zA-Z0-9._-]` admits `.` (period). Conjectural risk that future cmux versions reserve dotted workspace names. Verify against the pinned cmux version (OI-1); if uncertain, drop `.` from the set. | devops DOR-010 | Anecdotal |
  | 18 | Implementation guidance | Pre-sanitized basename must never appear in operator-visible terminal output. D11 sanitization plus consistent use of the sanitized value is the implementation contract; code review enforcement only. | devops DOR-011 | Evidenced |
  | 19 | Integration boundary | The integration shape between standard preflight and cmux preflight is unspecified. Discovery notes recommend a separate `cmuxctl.Preflight(...)` call invoked from `main()` after `startup()` returns successfully. Decision needed for the plan. | behavioral B8 | Evidenced |
  | 20 | Documentation / versioning | Phase 1 PR must include the MINOR version bump (0.10.0 → 0.11.0) AND the cmux setup how-to AND a feature doc AND CLAUDE.md updates — per `coding-standards/versioning.md`, `coding-standards/documentation.md`, and parent D25. Two coding standards are simultaneously violated if omitted. | devops DOR-009, junior Q7 | Evidenced |
  | 21 | Documentation / dependency | OI-1 (pinned cmux version) is transitively a Phase 1 shipping blocker via item #20: the setup how-to cannot say "install cmux version X" without resolving OI-1. Build-outline recommendation: pin the current latest cmux release. | devops DOR-012, junior Q2 | Evidenced |
  | 22 | Behavior gap | The named "pr9k is not a cmux descendant" precondition error cannot fire when cmux is configured in "allow-all" access mode (parent T2). Pure socket-open admits any local process under allow-all; the spec's named error condition is unreachable in that configuration. Either accept that the named error only fires under default access mode, or add a process-tree walk / access-mode probe. | junior Q1 | Evidenced |
  | 23 | Risk acceptance | Method-presence capability check is sufficient for renamed/missing methods but not for return-shape changes (a renamed JSON field, an added required field). Acceptance level needs to be stated explicitly. | junior Q2 | Anecdotal |
  | 24 | UX / convention | Should `--cmux` ship with `Hidden: true` until Phase 2? api-design.md says hide incomplete commands. Phase 1 does real work but produces no real workflow output — operators may file false bug reports against placeholder content. | junior Q3 | Evidenced |
  | 25 | Documentation | Validator runs in Phase 1 even though no steps execute → an operator with an empty/partial `config.json` cannot demo Phase 1. Spec needs to document this prerequisite OR exempt cmux mode from validator (would contradict D8). | junior Q4 | Evidenced |
  | 26 | Implementation choice | Placeholder process unsettled — shell one-liner (e.g., `printf ... ; sleep infinity`) vs. `pr9k` subcommand (e.g., `pr9k display placeholder header`). Has documentation/PATH implications either way. | junior Q6 | Evidenced |
  | 27 | Specialist handoff | Junior recommends pulling in `adversarial-security-analyst` for the cmux-diagnostic-to-terminal path. First external-data-to-terminal flow in the codebase; `internal/ansi.StripAll`'s scope (CSI/OSC/bare ESC/two-byte) needs an adversarial pass before Phase 1 ships. | junior Q9 | Evidenced |
  | 28 | Operator visibility | Phase 1 introduces orphan-creation failure modes (double-signal, partial-setup, SIGHUP-not-forwarded) but defers the orphan startup advisory to Phase 7. Phase 1 how-to / release notes should document orphan recognition (the `pr9k-` prefix in cmux's workspace list) so operators have a manual path. | junior Q10 | Evidenced |

- **Open Questions raised** (items not deterministically settled in R1):

  - **OQ-R1-1** (rows #1, also touches #16): How does pr9k actually observe placeholder process exits given cmux's documented API surface? Is there a pane-exit subscription, a per-pane status query, or must Phase 1 fall back to workspace-list-only observation (and accept the "close-each-pane gesture" cannot be observed)? — `spec-level`
  - **OQ-R1-2** (row #22): How is the "pr9k is not a cmux descendant" precondition actually verified — pure socket-open OR walk the process tree for a cmux ancestor? Pure socket-open silently passes under cmux's "allow-all" access mode. — `spec-level`
  - **OQ-R1-3** (row #12): Does parent D15's "every cmux API call timeout is fatal" rule apply uniformly to D9 polling calls during the waiting phase, or are waiting-phase polls exempt? — `plan-level` (clarification of D15's intended scope; resolvable by evidence/PM)
  - **OQ-R1-4** (row #14): The exit-0/non-zero distinction needs deterministic observation ordering OR the rule should be relaxed. Which? — `plan-level`
  - **OQ-R1-5** (row #15): Does the "no orphan from this failure mode" claim survive `workspace.close` failing for a non-race reason? Audit the partial-setup teardown guarantee. — `plan-level`
  - **OQ-R1-6** (row #24): Should `--cmux` ship with `Hidden: true` in Phase 1? — `plan-level`
  - **OQ-R1-7** (row #26): Placeholder process — shell one-liner OR `pr9k` subcommand? — `plan-level`
  - **OQ-R1-8** (row #27): Engage `adversarial-security-analyst` for the cmux-diagnostic-to-terminal path? — specialist-handoff
  - **OQ-R1-9** (row #16): Pane spawn order — commit to an explicit order (recommendation: orchestrator-first so operator never sees a partial visible scaffold). — `plan-level`
  - **OQ-R1-10** (row #17): D11 character set — keep `.` or drop it? Defer until OI-1 resolves the pinned cmux version. — `plan-level`
  - **OQ-R1-11** (row #23): Method-presence vs. return-shape skew — accept the risk explicitly OR commit to schema validation in `internal/cmuxctl`? — `plan-level`

- **Spec-maturity tags:**
  - `T#-contradiction`: 0
  - `spec-level`: 2 (OQ-R1-1, OQ-R1-2). OQ-R1-1 raised by 2 distinct specialists (behavioral B10, junior Q5); OQ-R1-2 raised by 1 specialist (junior Q1).
  - `plan-level`: 9 (OQ-R1-3, -4, -5, -6, -7, -9, -10, -11) plus 1 specialist-handoff (OQ-R1-8).
  - **Spec-maturity gate did NOT trip.** Gate trips only when (a) ≥ 2 `T#-contradictions` raised by ≥ 2 specialists OR (b) ≥ 5 `spec-level` findings raised by ≥ 3 specialists. Neither condition met. No PM facilitation pass needed for R1.

- **Resolution source:** All Open Questions deferred to R2 (iterative resolution loop). R2 will: engage `adversarial-security-analyst` per OQ-R1-8; resolve OQ-R1-1/-2 via evidence (re-read parent T1, investigation.md API table) and `junior-developer` reframing; surface OQ-R1-4/-6/-7 to user with recommendations after evidence/reframing if those don't settle them.

- **Decisions produced:** D-1 (cmux preflight integration shape), D-3 (pane spawn order — initial commitment finalized in R2), D-5 (single-goroutine RPC queue), D-6 (500ms polling cadence — initial commitment finalized in R2), D-7 (D15 timeout scope on D9 polls — initial commitment finalized in R2), D-8 (self-close double-fire mitigation), D-9 (two-goroutine watchdog+cleanup signal pattern), D-10 (SIGHUP registration — confirmed by SEC-005 in R2), D-11 (best-effort teardown — initial commitment finalized in R2), D-13 (method-presence capability check accepted — initial commitment finalized in R2), D-16 (skip logger and Bubble Tea on cmux Phase 1 path), D-17 (D11 sanitization keeps `.` for now — initial commitment finalized in R2), D-18 (`internal/cmuxctl` package layout), D-19 (per-pane exit via `surface.list` — initial commitment finalized in R2), D-20 (descendant probe via socket-open — initial commitment finalized in R2), D-21 (per-call timeout protocol), D-22 (goroutine cancellation discipline), D-23 (pre-sanitized basename never appears in terminal output), D-24 (Phase 1 PR scope), D-25 trivial (`--cmux` CLI flag wiring), D-26 trivial (version bump 0.10.0 → 0.11.0), D-27 trivial (preserve `StripAll` contract).
- **Changed in plan:** Architecture and Integration Points (entry-point branch, new `internal/cmuxctl` package, existing-package modifications); Runtime Behavior (launch sequence, dismissal sequence, signal handling); External Interfaces (CLI flag, cmux JSON-RPC, environment variables); Decomposition and Sequencing (U1, U2, U3, U4, U5, U6, U7 first drafts); RAID Log (R1, R2, R3, R4, R5 first drafts; assumptions A1, A3, A4); Testing Strategy (test-engineer T1–T14 plan absorbed); Operational Readiness (poll-cadence cost analysis, observability surfaces); Definition of Done (build-outline 5-step demo plus test-plan checkmarks); Open Items (OI-1 promoted to release-readiness blocker via DOR-009/DOR-012).
- **Project-manager next-step recommendation:** **Continue iterating** (Round 2). Engage `adversarial-security-analyst` (named handoff per OQ-R1-8). Apply evidence/junior-reframing on the remaining R1 Open Questions in the same round.

## R2: Security pass + evidence-based resolution of R1 Open Questions

- **Specialists engaged:** `adversarial-security-analyst` (named handoff from junior Q9 / OQ-R1-8). No re-engagement of behavioral / concurrency / devops specialists; their R1 findings were Evidenced with concrete plan-level remedies that synthesis can absorb directly. Evidence-based resolution applied to R1 Open Questions in this round per Step 6.
- **New input provided:** R1 claim ledger (consolidated above), with focused brief on the cmux-diagnostic-to-terminal path, the `internal/ansi.StripAll` actual implementation in `src/internal/ansi/strip.go`, the `CMUX_SOCKET_PATH` env-var trust surface, and the placeholder-process spawn shape.
- **Claim ledger** (consolidated; new findings from R2 + resolutions for R1 Open Questions):

  ### New findings (security-analyst)

  | # | Category | Claim | Specialist | State |
  |---|----------|-------|------------|-------|
  | SEC-001 | Terminal injection | `StripAll` passes raw C0 control bytes (CR `0x0d`, BS `0x08`, DEL `0x7f`, VT, FF, BEL, NUL) through unmodified. Demonstrated exploit: cmux diagnostic `"workspace.create failed\rpr9k: workspace created: pr9k-myrepo-<ts>"` overwrites the error line with a fake success message via CR overstrike. Cited `src/internal/ansi/strip.go:14` and pinned by `src/internal/ansi/strip_test.go:267-288`. Medium severity. | adversarial-security SEC-001 | Evidenced |
  | SEC-002 | Terminal injection (cosmetic) | Double-ESC bypass emits `[31mred` literal but is not actually exploitable — the leaked `[` doesn't reactivate stripped CSI. Document the contract in a code comment. | adversarial-security SEC-002 | Evidenced |
  | SEC-003 | SSRF / env-var trust / T2 bypass | `CMUX_SOCKET_PATH` is fully trusted: pr9k will `net.Dial` whatever path the env var points to and treat well-formed JSON-RPC 2.0 responses as authoritative cmux responses. Defeats the entire T2 ancestry-based access model. Demonstrated path: attacker-controlled fake socket + `nc -lU` + canned responses → pr9k prints fake workspace-name confirmation, exits 0, no real cmux workspace ever appeared. | adversarial-security SEC-003 | Evidenced |
  | SEC-004 | Terminal injection | `StripAll` does not strip 8-bit C1 controls (`0x80–0x9F`) — pinned by `strip_test.go:91-104` ("document the gap"). Demonstrated exploit: cmux diagnostic containing `\x9d0;root@attackserver\x07` (C1 OSC, code 0, fake title, BEL terminator) sets the operator's terminal window title to `root@attackserver` on xterm-compatible terminals (most macOS Terminal.app and iTerm2 configurations). Title-phishing primitive. Medium severity. | adversarial-security SEC-004 | Evidenced |
  | SEC-005 | Signal handling | Confirms R1 row #7 from a security frame: SIGHUP not registered → guaranteed orphan workspace on every operator terminal-close gesture (when shell forwards SIGHUP). Phase 1 has only annoyance-level consequences with shell-one-liner placeholders; severity rises in later phases when placeholder is `pr9k display ...`. | adversarial-security SEC-005 | Evidenced |

  ### Resolutions applied to R1 Open Questions

  | OQ | Resolution source | Resolution |
  |----|-------------------|------------|
  | OQ-R1-1 (placeholder-exit observation mechanism) | evidence | Spec edge-case "the pane shows the placeholder's exit status per cmux's standard pane-lifecycle behavior" + parent T5 ("a pane whose process exits remains in the workspace, showing the exit status to the operator, until explicitly closed") together support that cmux tracks per-pane exit status observably. The investigation API table includes `surface.list` which (by parallel with `workspace.list`) is the natural query surface. **Plan commits to**: implementation must verify cmux exposes per-pane exit status via `surface.list` (or equivalent); if it does not, document the degradation to workspace-list-only and surface as a Phase 1 implementation precondition (similar to D18 capability check). |
  | OQ-R1-2 (descendants check verification) | evidence | The named precondition error "pr9k is not a cmux descendant" is meaningful only under cmux's default access mode (descendants-only). Under "allow-all" mode the named error is unreachable but pr9k still works (cmux admits the call). **Plan commits to**: use socket-open as the descendant probe; document in the cmux setup how-to that under allow-all access mode the "not a descendant" error never fires (operator policy, not a hard pr9k precondition). |
  | OQ-R1-3 (D15 timeout scope on D9 polls) | evidence | Parent D15's "fatal timeout" was specified for the launch sequence and workflow-driving calls. Treating a single transient `workspace.list` poll timeout as fatal would tear down a healthy workspace mid-inspection (poor operator experience). **Plan commits to**: D15's fatal rule applies to launch and workflow-driving calls; D9 poll timeouts during the waiting phase are tolerated up to N=3 consecutive fires, then escalate to fatal teardown. |
  | OQ-R1-4 (exit-code distinction on dismissal) | user input (resolved) | **User decision: drop the distinction.** Every dismissal observation produces exit zero. Reserve non-zero for actual failures (preflight error, partial-setup teardown, signal-driven shutdown, observation-poll timeout escalation). The spec's User Interactions Dismissal-gestures bullet ("zero for workspace-close path; non-zero for close-pane path") is amended by this implementation decision; synthesis records the contradiction with the spec and the user's resolution explicitly. |
  | OQ-R1-5 (no-orphan guarantee audit) | evidence + reframing | Plan commits to "best-effort teardown" with operator-visible diagnostic: try `workspace.close` once during teardown; on failure, log "orphan workspace `<name>` could not be closed; dismiss it manually via cmux's controls" to launching terminal; continue to focus-restore; exit non-zero. The spec's "no orphan" claim is qualified rather than contradicted. |
  | OQ-R1-6 (`--cmux` Hidden flag) | evidence + reframing | api-design.md's "Hidden until complete" rule defines incomplete as "wired to a stub or placeholder." Phase 1's `--cmux` does real work (workspace lifecycle is fully functional); the placeholder content is in the panes, not the flag. **Plan commits to**: ship `--cmux` visible with help text naming Phase 1's scope (e.g., "experimental: launches a four-pane placeholder workspace; full workflow rendering ships in a later release"). |
  | OQ-R1-7 (placeholder process choice) | evidence + YAGNI | Shell one-liner avoids new code, new convention, new PATH dependency, and a new (potentially Hidden) cobra subcommand that would be deleted when Phase 2 ships real renderers. **Plan commits to**: shell one-liner placeholders. Specifically: visible panes get `printf "<role> — Phase 1 placeholder\n" && tail -f /dev/null`; orchestrator gets `tail -f /dev/null` only (no visible output per D1). Use POSIX-portable `tail -f /dev/null` rather than `sleep infinity` (GNU extension). cmux's `surface.spawn` (or equivalent) takes argv directly — no shell quoting; pr9k composes the argv as `["sh","-c","printf '<role> — Phase 1 placeholder\\n' && tail -f /dev/null"]`. |
  | OQ-R1-8 (engage adversarial-security-analyst) | resolved | Engaged in this round; produced SEC-001 through SEC-005. |
  | OQ-R1-9 (pane spawn order) | evidence | Orchestrator-first: hidden orchestrator pane created first via initial workspace.create or first surface.split; visible panes split off subsequently. Operator never sees a partial visible scaffold during teardown. **Plan commits to**: orchestrator-first spawn order. |
  | OQ-R1-10 (D11 character set — keep `.` or drop) | deferred | Defer until OI-1 resolves the pinned cmux version. Plan uses D11's full set as initial implementation; flag as a Phase 1 release-readiness check tied to OI-1. |
  | OQ-R1-11 (method-presence vs. return-shape skew) | evidence + YAGNI | D18 explicitly accepts method-presence checking. Schema-shape validation has no concrete trigger (no incident, no named cmux release that broke shape). Standard `json.Unmarshal` with struct tags handles unknown/missing fields via zero values. **Plan accepts the risk explicitly**: schema-shape validation is a YAGNI candidate; reopen trigger = a cmux release breaks a method's response shape and the resulting confusing error costs operator time. |

- **Open Questions raised (R2):**
  - **OQ-R2-1** (from SEC-001 + SEC-004): The existing `internal/ansi.StripAll` is insufficient for the cmux-diagnostic-to-terminal path — passes through C0 cursor-movement bytes (CR/BS/DEL/etc.) and 8-bit C1 controls (`0x9B` C1 CSI, `0x9D` C1 OSC). **Recommended resolution:** add a `StripForTerminalOutput` (or `StripStrict`) variant to `internal/ansi` that strips ESC sequences PLUS the named C0 cursor-movement bytes PLUS the 8-bit C1 controls (consuming any C1 OSC/DCS payloads to BEL or `0x9C` ST). Apply only on the cmux-diagnostic path; preserve existing `StripAll` contract for its current callers (workflowio recovery view). — `plan-level`
  - **OQ-R2-2** (from SEC-003): `CMUX_SOCKET_PATH` is currently fully trusted and defeats the T2 ancestry model entirely. **Recommended resolution:** validate the env-var path before `net.Dial` (non-empty, resolves via `EvalSymlinks` to a Unix-socket-type file in a trusted prefix `/tmp/` OR the platform socket convention OR no env var override allowed). After connecting, call `system.identify` (already part of D18 capability check) and require the response to identify as cmux. The "offending socket path" printed in the not-a-descendant error message must be the resolved (symlink-expanded) path so the operator can see redirection. — `plan-level`

- **Spec-maturity tags (R2 cumulative):**
  - `T#-contradiction`: 0
  - `spec-level` (R1 + R2): 0 remaining (OQ-R1-1 and OQ-R1-2 retired to plan-level via R2 evidence).
  - `plan-level`: SEC-001, SEC-002, SEC-003, SEC-004, SEC-005 (all evidenced); OQ-R2-1, OQ-R2-2; plus all R1 plan-level items.
  - **One spec-contradiction OQ remains for user input**: OQ-R1-4 (exit-code distinction). Two viable resolutions; plan recommendation provided; user picks.
  - **Spec-maturity gate did NOT trip** in R2.

- **Resolution source per OQ:** see resolution table above (mostly evidence + reframing; OQ-R1-4 escalates to user).

- **Decisions produced:** D-2 (`--cmux` ships visible with experimental help text — finalized via OQ-R1-6 evidence/reframing), D-3 (pane spawn order — finalized as orchestrator-first via OQ-R1-9 evidence), D-4 (placeholder process is shell one-liner — finalized via OQ-R1-7 evidence + YAGNI), D-6 (500ms polling cadence — finalized via OQ-R2-1 evidence on `surface.list`), D-7 (D15 timeout scope — finalized via OQ-R1-3 evidence), D-10 (SIGHUP registration — confirmed by SEC-005), D-11 (best-effort teardown with operator-visible diagnostic — finalized via OQ-R1-5 evidence + reframing), D-12 (drop the exit-code distinction on dismissal — **user input** via OQ-R1-4; spec contradiction recorded), D-13 (method-presence capability check accepted, schema-shape deferred — finalized via OQ-R1-11 evidence + YAGNI), D-14 (NEW: `StripForTerminalOutput` ANSI variant — produced from SEC-001 + SEC-004), D-15 (NEW: `CMUX_SOCKET_PATH` validation — produced from SEC-003), D-17 (D11 sanitization keeps `.` — finalized via OQ-R1-10 deferral to OI-1), D-19 (per-pane exit observation via `surface.list` — finalized via OQ-R1-1 evidence with documented degradation path), D-20 (descendant probe via socket-open with allow-all caveat — finalized via OQ-R1-2 evidence).
- **Changed in plan:** Security Posture (full section produced from SEC-001 through SEC-005); Architecture and Integration Points (preflight CMUX_SOCKET_PATH validation step added); Runtime Behavior (exit-code rule rewritten per [D-12](implementation-decision-log.md#d-12-drop-the-exit-code-distinction-on-dismissal--every-dismissal-observation-produces-exit-zero); spec-contradiction note added; pane-spawn order finalized as orchestrator-first; per-call timeout escalation rule N=3 added; teardown best-effort behavior added); External Interfaces (`--cmux` help text + visibility commitment finalized; `CMUX_SOCKET_PATH` validation hook); Decomposition and Sequencing (U8 added for ANSI hardening + env-var validation; U3 expanded with `CMUX_SOCKET_PATH` validation; U4 placeholder shell-one-liner argv); Deferred (YAGNI) section produced (YAGNI-1 through YAGNI-5); Open Items (OI-A added recording the spec contradiction from D-12 / OQ-R1-4); Specialist Handoffs (`adversarial-security-analyst` added for code review of `StripForTerminalOutput` and `CMUX_SOCKET_PATH` validation); RAID Log (assumptions A2, A5 added; risk R4 added).
- **Project-manager next-step recommendation:** **Blocked pending user input on OQ-R1-4** (the only spec-contradiction). All other OQs resolved by evidence/reframing/YAGNI. After user input on OQ-R1-4, proceed to Step 7.5 (YAGNI sweep) and Step 8 (synthesis). Round cap reached for medium (R1 + R2).
