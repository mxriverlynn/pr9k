package uichrome

const (
	// MinTerminalWidth is the minimum terminal width the TUI is designed for.
	MinTerminalWidth = 60

	// MinTerminalHeight is the minimum terminal height the TUI is designed for.
	MinTerminalHeight = 16

	// DialogMaxWidth is the maximum inner width for centered dialog overlays (D36).
	DialogMaxWidth = 72

	// DialogMinWidth is the minimum inner width for centered dialog overlays (D36).
	DialogMinWidth = 30

	// HelpModalMaxWidth is the maximum inner width for the help modal overlay.
	HelpModalMaxWidth = 72
)
