// Package version exposes the pr9k application version as a compile-time
// constant. Keep this file as the single source of truth: CLI --version output,
// the TUI footer, and any future release tooling should read Version from here.
package version

// Version is the current pr9k release version.
const Version = "0.7.5"
